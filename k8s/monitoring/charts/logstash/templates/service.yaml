apiVersion: v1
kind: Service
metadata:
  name: logstash-service
  namespace: monitoring
  labels:
    app: logstash
spec:
  selector:
    app: logstash
  ports:
    - name: beats
      port: 5044
      targetPort: 5044
    - name: tcp
      port: 5000
      targetPort: 5000
  type: ClusterIP
