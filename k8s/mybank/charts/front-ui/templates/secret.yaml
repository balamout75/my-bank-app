apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-front-ui
  labels:
    app: front-ui
    app.kubernetes.io/name: front-ui
    app.kubernetes.io/instance: {{ .Release.Name }}
type: Opaque
stringData:
  OAUTH2_CLIENT_SECRET: {{ .Values.keycloak.clientSecret }}
